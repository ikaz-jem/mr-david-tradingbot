import Link from "next/link";

export function LegalDocument({ title, intro, sections }: { title: string; intro: string; sections: { title: string; paragraphs: string[] }[] }) {
  return <main className="mx-auto max-w-4xl px-5 py-20 sm:px-8 lg:py-28"><p className="eyebrow">Product documents</p><h1 className="display mt-5 text-5xl sm:text-6xl">{title}</h1><div className="mt-7 rounded-xl border border-[#8f6e36] bg-[#8f6e3619] p-4 text-sm leading-6 text-[#f3dfb8]"><strong>Draft for review.</strong> This page is part of the product prototype. It must be reviewed for the launch entity and eligible countries before public paid or live trading access.</div><p className="mt-9 text-lg leading-8 text-[#c0cbc0]">{intro}</p><div className="mt-12 space-y-11">{sections.map((section, index) => <section key={section.title} className="border-t border-line pt-8"><div className="flex gap-5"><span className="pt-1 font-mono text-sm text-accent">{String(index + 1).padStart(2, "0")}</span><div><h2 className="text-2xl font-bold tracking-tight">{section.title}</h2>{section.paragraphs.map(paragraph => <p key={paragraph} className="mt-4 text-base leading-8 text-muted">{paragraph}</p>)}</div></div></section>)}</div><div className="mt-14 border-t border-line pt-6 text-sm text-muted">Questions? <Link href="/contact" className="text-accent underline">Contact us</Link>.</div></main>;
}
