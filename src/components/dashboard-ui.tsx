import Link from "next/link";
import { ArrowUpRight, Inbox, type LucideIcon } from "lucide-react";

export function PageIntro({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: { label: string; href: string } }) { return <div className="workspace-intro relative mb-7 flex flex-col justify-between gap-6 overflow-hidden rounded-[22px] border border-[#48633b] px-6 py-7 sm:flex-row sm:items-end sm:px-8 sm:py-8"><div className="relative z-10"><p className="eyebrow">{eyebrow}</p><h1 className="display mt-4 text-3xl font-bold sm:text-[2.6rem]">{title}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-[#b7c8b0]">{description}</p></div>{action && <Link href={action.href} className="button-primary relative z-10 inline-flex h-11 shrink-0 items-center justify-center gap-2 rounded-xl px-4 text-sm">{action.label}<ArrowUpRight className="size-4"/></Link>}<span aria-hidden="true" className="workspace-intro-orbit absolute -right-10 -top-24 hidden size-64 rounded-full border border-[#bbff3540] sm:block"/></div>; }
export function StatCard({ label, value, detail, icon: Icon, accent = false }: { label: string; value: string; detail: string; icon: LucideIcon; accent?: boolean }) { return <div className={`workspace-stat rounded-[18px] border p-5 ${accent ? "border-[#719744] bg-[#1a2a19]" : "border-[#354933] bg-[#111b13]"}`}><div className="flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-[.08em] text-[#a8bca1]">{label}</span><span className="flex size-9 items-center justify-center rounded-xl border border-[#47643a] bg-[#c5ff4111]"><Icon className="size-4 text-accent"/></span></div><div className="mt-6 text-[2.2rem] font-bold tracking-tight number">{value}</div><p className="mt-1 text-xs text-muted">{detail}</p></div>; }
export function EmptyState({ title, text, icon: Icon = Inbox, action }: { title: string; text: string; icon?: LucideIcon; action?: { label: string; href: string } }) { return <div className="flex min-h-[270px] flex-col items-center justify-center rounded-[18px] border border-dashed border-[#354538] bg-[#111915] px-5 py-12 text-center"><span className="flex size-12 items-center justify-center rounded-xl border border-[#405941] bg-[#c5ff4112] text-accent"><Icon className="size-5"/></span><h2 className="mt-5 text-lg font-bold">{title}</h2><p className="mt-2 max-w-sm text-sm leading-6 text-muted">{text}</p>{action && <Link href={action.href} className="mt-6 inline-flex items-center gap-1.5 text-sm font-bold text-accent hover:underline">{action.label}<ArrowUpRight className="size-4"/></Link>}</div>; }
export function SectionHeader({ title, detail, href, linkText }: { title: string; detail?: string; href?: string; linkText?: string }) { return <div className="mb-5 flex items-center justify-between gap-4"><div><h2 className="text-lg font-bold tracking-tight">{title}</h2>{detail && <p className="mt-1 text-xs text-muted">{detail}</p>}</div>{href && <Link href={href} className="flex items-center gap-1 text-xs font-bold text-accent hover:underline">{linkText ?? "View all"}<ArrowUpRight className="size-3.5"/></Link>}</div>; }
